server:
  port: 8201
spring:
  application:
    name: consumer-feign
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

feign:
   hystrix:
    enabled: true

# management.endpoints.web.exposure.include=hystrix.stream


debug: false



